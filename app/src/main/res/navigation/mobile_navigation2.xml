<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation2"
    app:startDestination="@+id/navigation_calendario">

    <fragment
        android:id="@+id/navigation_calendario"
        android:name="com.example.clinica_psicologicaa.ui.home.HomeFragment"
        android:label="Calendario"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_navigation_calendario_to_navigation_dashboard"
            app:destination="@id/navigation_dashboard" />
    </fragment>

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="com.example.clinica_psicologicaa.ui.dashboard.DashboardFragment"
        android:label="Realizar una cita"
        tools:layout="@layout/fragment_dashboard" >
        <action
            android:id="@+id/action_navigation_dashboard_to_navigation_notifications"
            app:destination="@id/navigation_notifications" />
    </fragment>

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.example.clinica_psicologicaa.ui.notifications.NotificationsFragment"
        android:label="Cerrar sesion"
        tools:layout="@layout/fragment_notifications" >
        <action
            android:id="@+id/action_navigation_notifications_to_materiales"
            app:destination="@id/materiales" />
    </fragment>
    <fragment
        android:id="@+id/materiales"
        android:name="com.example.clinica_psicologicaa.Materiales"
        android:label="Materiales"
        tools:layout="@layout/fragment_materiales" >
        <action
            android:id="@+id/action_materiales_to_misnotas"
            app:destination="@id/misnotas" />
    </fragment>
    <fragment
        android:id="@+id/misnotas"
        android:name="com.example.clinica_psicologicaa.Misnotas"
        android:label="Mis notas"
        tools:layout="@layout/fragment_misnotas" />
</navigation>